{
  "name": "site",
  "version": "1.0.0",
  "private": true,
  "description": "Build-time tooling for the Jekyll site (mermaid diagram pre-rendering).",
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.4.2"
  }
}
